{
  "name": "tac-crypto-payment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter yango-demo-app dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter yango-demo-app preview",
    "type-check": "pnpm -r type-check",
    "lint": "run-s lint:*",
    "lint:oxlint": "oxlint . --fix",
    "lint:eslint": "eslint . --fix --cache"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^5.10.0",
    "@tsconfig/node24": "catalog:workspace",
    "@types/node": "catalog:workspace",
    "@vue/eslint-config-typescript": "^14.7.0",
    "@vue/tsconfig": "catalog:workspace",
    "eslint": "^10.2.0",
    "eslint-plugin-oxlint": "~1.57.0",
    "eslint-plugin-vue": "~10.8.0",
    "jiti": "^2.6.1",
    "npm-run-all2": "catalog:workspace",
    "oxlint": "~1.57.0",
    "typescript": "catalog:workspace",
    "vite": "catalog:vite"
  },
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": "^24.0.0"
  }
}
